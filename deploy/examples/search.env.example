# Search-time configuration. Keep this file outside the web root.
MEILI_URL=http://127.0.0.1:7700
SEARCH_KEY=replace-with-search-only-key
WRITE_KEY=
MEILI_INDEX_LIVE=posts_live

# The rebuild entrypoint uses a separate file with REBUILD_KEY/TASK_KEY.
